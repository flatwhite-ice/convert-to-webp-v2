# PRD — convert-to-webp2 (Windows GUI 변환기)

## 1. 개요
사진가를 위해 JPG/JPEG 파일을 **WebP**(무손실)로 변환하는 **Windows 데스크톱 GUI** 도구.
기존 CLI 도구([`../convert-to-webp`](../convert-to-webp/))의 핵심 변환 로직(무손실 인코딩 + EXIF 보존)을 재활용하되, 비개발자도 쓸 수 있도록 GUI + 단독 실행 파일로 재구성한다.

## 2. 대상 사용자
- 사진가 등 **비개발자**. 파이썬/터미널 지식이 없어도 더블클릭으로 실행 가능해야 한다.

## 3. 기술 스택
| 항목 | 선택 | 비고 |
|------|------|------|
| 언어 | Python 3.9+ | 멀티 OS 지원 |
| GUI | PySide6 (Qt) | 네이티브에 가까운 외관 |
| 이미지 처리 | Pillow | WebP 무손실 인코딩 |
| EXIF | piexif (폴백) | 1순위는 raw EXIF bytes |
| 병렬 처리 | `ThreadPoolExecutor` | libwebp가 GIL 해제 → 스레드로도 멀티코어 활용 |
| 배포 | PyInstaller (`--onedir --windowed`) + zip | 폴더(exe + `_internal/`)를 zip으로 배포. 자가압축해제가 없어 백신/SmartScreen 오탐 감소 |

## 4. 기능 요구사항

### FR-1. 폴더 선택
- 사용자가 변환할 이미지가 들어있는 폴더를 GUI 다이얼로그로 선택한다.
- 선택된 폴더 경로를 화면에 표시한다.

### FR-2. 파일 목록 읽기
- 선택 폴더에서 `*.jpg`, `*.jpeg` 파일을 대소문자 구분 없이 수집한다.
- 중복 제거 후 발견된 파일 개수를 표시한다.
- (재귀 탐색 없음 — 선택 폴더 최상위만 대상)

### FR-3. 출력 폴더 생성
- 선택한 **타겟 폴더 하위에 `webp` 폴더**를 생성한다 (`target/webp/`).
- 이미 존재하면 재사용한다 (`exist_ok=True`).
- 동일 이름의 결과 파일이 있으면 덮어쓴다.

### FR-4. 변환 (무손실 + EXIF 유지)
- WebP 저장 설정: `lossless=True`, `quality=100`, `method=6` (픽셀 완전 보존).
- EXIF 보존:
  - 1순위: `img.info["exif"]` raw bytes (완전 보존)
  - 2순위(폴백): `piexif.dump(piexif.load(src))`
  - 저장 후 `Exif\x00\x00` 접두사 복원 (`_patch_webp_exif`) → Windows 탐색기/WIC 코덱 호환
- DPI 정보가 있으면 유지한다.
- 출력 파일명: `<원본 stem>.webp`.

### FR-5. 해상도 제한 (선택)
- "긴 축 최대 해상도 지정" 옵션을 켜고 픽셀 값을 입력할 수 있다.
- 켜진 경우, 긴 쪽 변이 입력값을 넘으면 비율을 유지한 채 `LANCZOS`로 **축소**한다.
- 입력값보다 작은 이미지는 **업스케일하지 않는다**.
- 꺼진 경우 원본 해상도를 유지한다.

### FR-6. 진행 표시 및 취소
- 변환 중 **프로그레스바**로 진행률을 표시한다 (완료/전체).
- 변환은 백그라운드 스레드에서 수행되어 UI가 멈추지 않는다.
- **취소 버튼**: 누르면 아직 시작되지 않은 작업은 취소하고, **진행 중인 파일만 변환을 완료**한 뒤 멈춘다.

### FR-7. 완료 요약
- 모든 변환(또는 취소) 종료 시 **전체 파일 개수**와 **변환 완료 개수**를 표시한다.
- 실패한 파일이 있으면 함께 표시한다.

## 5. 비기능 요구사항
- **멀티 OS**: 코드는 Windows/macOS/Linux에서 동작 (배포 산출물은 Windows `.exe` 우선).
- **무프리징**: 변환 중에도 UI 응답 유지 (취소/진행 갱신).
- **한글 표시**: GUI 텍스트는 한국어.

## 6. 범위 외 (Non-goals)
- PNG/TIFF 등 JPG 외 입력 포맷.
- 하위 폴더 재귀 탐색.
- 손실 압축 품질 슬라이더(현재는 무손실 고정).
- 변환 결과 미리보기/썸네일.

## 7. 동작 흐름
```
앱 실행
 └─ 폴더 선택 (FR-1)
     └─ JPG/JPEG 스캔, 개수 표시 (FR-2)
         └─ 해상도 옵션 설정 (FR-5)
             └─ [변환 시작]
                 ├─ target/webp/ 생성 (FR-3)
                 ├─ ThreadPoolExecutor로 병렬 변환 (FR-4)
                 │    └─ 프로그레스바 갱신 (FR-6)
                 │    └─ [취소] → 대기 작업 취소, 진행 중만 완료
                 └─ 완료 요약 표시 (FR-7)
```
